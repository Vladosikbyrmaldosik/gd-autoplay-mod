name: Build Geode Mod
on:
  push:
    branches: [main]
  workflow_dispatch:
jobs:
  build-android32:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: geode-sdk/build-geode-mod@main
        id: build
        with:
          sdk: nightly
          platform: android32
      - uses: actions/upload-artifact@v4
        with:
          name: AutoPlayer-android32
          path: ${{ steps.build.outputs.build-output }}
          if-no-files-found: warn

  build-android64:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: geode-sdk/build-geode-mod@main
        id: build
        with:
          sdk: nightly
          platform: android64
      - uses: actions/upload-artifact@v4
        with:
          name: AutoPlayer-android64
          path: ${{ steps.build.outputs.build-output }}
          if-no-files-found: warn
